# Disney Assistant Updates

Public update channel for the Android app. The source code remains in the private development repository.

- `latest.json` contains the current update metadata.
- APK files are published as GitHub Release assets.
- The app verifies the APK SHA-256 before opening Android's installer.
